#include "AccController.h"
#include "MainController.h"
#include <iostream>
using namespace std;

int main()
{
    MainController MAcc;
    MAcc.start();

    //startEmail()
    /* 프로그램 실행
    1. 회원가입 -> ID, PW, Back
    2. 로그인 -> 맞는지 확인 -> Show -> All
    3. 종료
    
    // 회원가입 or 로그인 선택

    // 로그인 체크 ->
    // 기능 선택 
    */

    //SignUP(email, password);
}